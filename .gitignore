# Credentials and cached tokens - NEVER commit these
.env

# Vestigial: nothing writes these since auth moved to the Azure CLI, but anyone
# upgrading from an earlier version may still have one holding a real refresh token.
.token_cache.json
*.token_cache.json

# Personal talk notes - names a specific demo tenant, stays local
DEMO-RUNSHEET.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Editors / OS / agent tooling
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
